Hello user!

I have comprehensively implemented the remaining fixes and architectural improvements identified in the MODULE 2 CHUNK ANALYSIS for the `brain/` module. 

Here is a summary of the accomplishments:
- **Dependency Injection (BrainModule)**: Converted all manual `@Provides` methods for Rules and Skills to properly use `@Binds` with `@IntoSet`. This guarantees Hilt correctly utilizes multibinding for discovery during runtime.
- **Agentic Orchestrator Constraints**: 
  - Refactored the `AgentTool` interface to support suspending execution natively (`suspend fun execute()`).
  - Purged all `runBlocking` invocations across the `DeepReasoningTool`, `RecallMemoryTool`, and `GetSystemStatusTool`, replacing them with safe coroutine handling.
  - Replaced the anti-pattern `NousApplication.instance` static calls by properly injecting the `AgentFacade` through the Orchestrator's constructor and passing it down to the tools.
  - Secured the `CalculateTool` parsing engine to handle arbitrary spacing and prevent parse crashes.
- **LLM and Data Pipelines**: 
  - Overhauled `CloudLlmLayer.kt` to securely route API Keys dynamically using the `CredentialVault`, offloaded all OkHttp synchronous calls to `withContext(Dispatchers.IO)`, and added strict JSON exception bounds.
  - Deferred `LocalLlmLayer`'s ActivityManager capture using `by lazy` to prevent startup timing issues, and swapped out the database `runBlocking` inside the watchdog thread for proper coroutine scoping.
- **System Guards**: 
  - Upgraded the `BrainPreProcessor`'s network checks to leverage the modern `NetworkCapabilities` API. 
  - Enforced strict immutability in `BrainContext` (`BrainBudget` fields are now `val`), and eliminated unsafe object casting loops in `BrainFacade`.

The application successfully compiles without warnings or blocked paths. I am ready for the next set of instructions or module testing phases!
